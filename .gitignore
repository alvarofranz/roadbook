.DS_Store
/.idea
/.vscode
/.fleet
/.claude
/node_modules
# /source holds local-only dev scripts, except the versioned release stamper
/source/*
!/source/stamp-version.mjs
/public/assets/fontawesome
/public/assets/js/config.js
/.env
/vendor
/storage
/public/avatars
/public/event-logos
/public/photos
/public/audio
/cron/cron.log
/bashy

# The private DB ops doc (DB.md): handed over out-of-band, never in this PUBLIC
# repo (it holds the production panel key, scoped to rdbk).
/DB.md

# Generic security/architecture report — untracked (issue #100): boilerplate with
# no secrets or grounded findings, no value being public in the repo.
/docs/secnotes.md

# Capacitor — the android/ (and ios/) projects are committed, but these are generated:
/public/assets/js/native.bundle.js
/ios/App/Pods
/ios/App/App/public
/android/.gradle
/android/build
/android/app/build
/android/app/src/main/assets/public
/android/local.properties
/android/.idea
/android/capacitor-cordova-android-plugins
/android/keystore.properties
/android/rdbk-upload.jks
/temp
