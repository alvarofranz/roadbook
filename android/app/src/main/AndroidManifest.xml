<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links (#268): an installed app opens rdbk.app links instead of the browser
                 (the /go/<code> event join + event / public-roadbook pages). autoVerify checks
                 https://rdbk.app/.well-known/assetlinks.json against the signing cert. Each prefix
                 ends in a slash, like the iOS apple-app-site-association paths, so /events or
                 /goanything stay in the browser. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="rdbk.app" />
                <data android:pathPrefix="/go/" />
                <data android:pathPrefix="/event/" />
                <data android:pathPrefix="/challenge/" />
                <data android:pathPrefix="/reader/" />
                <data android:pathPrefix="/editor/" />
            </intent-filter>

            <!-- Files (#996): a .gpx or a .rdbk tapped in Files, a download or a chat opens in the app
                 (the bridge reads its first bytes: a roadbook → the Reader, a GPX → the Editor). By
                 type — Android names neither extension, so most senders say octet-stream — and, for
                 the ones that pass a path, by name. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/gpx" />
                <data android:mimeType="application/x-gpx+xml" />
                <data android:mimeType="application/x-roadbook" />
                <data android:mimeType="application/octet-stream" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <!-- pathPattern stops at the first dot it meets, so a name with more dots needs its own -->
                <data android:pathPattern=".*\\.gpx" />
                <data android:pathPattern=".*\\..*\\.gpx" />
                <data android:pathPattern=".*\\..*\\..*\\.gpx" />
                <data android:pathPattern=".*\\.rdbk" />
                <data android:pathPattern=".*\\..*\\.rdbk" />
                <data android:pathPattern=".*\\..*\\..*\\.rdbk" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Camera: QR code scanning for the event participants page (#311). -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- A Google Play Services library auto-merges the advertising-ID permission; RDBK shows no
         ads and declares no ad-ID use in Play, so strip it or Play rejects the release. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
</manifest>
